### `react-frontend/README.md`
```md
# NerdNest — Frontend (React + Vite)

React app for NerdNest.

## Quick start
```bash
npm install
cp .env.example .env   # optional: set VITE_API_URL
npm run dev            # opens local dev server
